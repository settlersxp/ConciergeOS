The workspace has all the libraries. Instead of guessing parameters and solutions use the library's code to come up with a solution.
The fix provided must satisfy the tests found in the "docker" folder.
You are allowed to propose changes in the tests if you can prove using the library's code that the test is wrong.