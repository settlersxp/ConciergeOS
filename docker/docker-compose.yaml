services:
  caddy:
    image: caddy:2-alpine
    container_name: caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "2019:2019"
    volumes:
      - ./Caddyfile.json:/etc/caddy/Caddyfile.json
      - caddy_data:/data
      - caddy_config:/config
    command: caddy run --config /etc/caddy/Caddyfile.json
    depends_on:
      - frontend
      - keycloak
      - oidc-main
    networks:
      - app-network

  oidc-main:
    image: quay.io/oauth2-proxy/oauth2-proxy:v7.15.3
    container_name: oidc-main
    restart: unless-stopped
    volumes:
      - ./oidc-main.toml:/cfg.toml
    extra_hosts:
      - "out-customer.com:host-gateway"
    environment:
      - OIDC_REALM=${OIDC_REALM:-production}
    command: >
      oauth2-proxy --config /cfg.toml
      --oidc-issuer-url=https://out-customer.com/auth/realms/${OIDC_REALM:-production}
    depends_on:
      - keycloak
      - frontend
      - valkey
    networks:
      - app-network

  role-sync:
    image: python:3.12-slim
    container_name: role-sync
    restart: unless-stopped
    volumes:
      - ./role_sync.py:/app/role_sync.py
      - ./rbac_routes.yaml:/app/rbac_routes.yaml
    environment:
      - KEYCLOAK_URL=http://keycloak:8080/auth
      - KEYCLOAK_REALM=${OIDC_REALM:-production}
      - KEYCLOAK_ADMIN_USER=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin
      - CADDY_ADMIN_URL=http://caddy:2019
      - SYNC_INTERVAL=10
      - VALKEY_URL=redis://valkey:6379/0
      - SESSION_COOKIE_NAME=_oauth2_proxy
    command: >
      sh -c "pip install requests pyyaml valkey -q && python3 -u /app/role_sync.py"
    depends_on:
      - keycloak
      - caddy
      - valkey
    networks:
      - app-network

  valkey:
    image: valkey/valkey:8-alpine
    container_name: valkey
    restart: unless-stopped
    networks:
      - app-network

  frontend:
    image: concos-frontend:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile.frontend
    container_name: frontend
    restart: unless-stopped
    networks:
      - app-network

  backend:
    image: concos-backend:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile.backend
    container_name: backend
    restart: unless-stopped
    networks:
      - app-network

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: keycloak
    restart: unless-stopped
    volumes:
      - keycloak_data:/opt/keycloak/data
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HOSTNAME: "https://out-customer.com/auth"
      KC_HTTP_RELATIVE_PATH: /auth
      KC_HTTP_ENABLED: "true"
    command: start-dev
    ports:
      - "8080:8080"
    networks:
      - app-network

volumes:
  caddy_data:
  caddy_config:
  keycloak_data:

networks:
  app-network:
    driver: bridge